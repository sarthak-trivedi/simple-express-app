<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Message Fetcher</title>
</head>
<body>
    <h1>Fetched Message:</h1>
    <p><%= message %></p>

    <h2>Previously Saved Messages</h2>
    <ul>
        <% notes.forEach(note => { %>
            <li><%= note.text %></li>
        <% }) %>
    </ul>
</body>
</html>
